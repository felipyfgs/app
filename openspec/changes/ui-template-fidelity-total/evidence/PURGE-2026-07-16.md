# Purga de chrome custom — 2026-07-16

> **Status: DIREÇÃO APROVADA / IMPLEMENTAÇÃO PARCIAL.** A decisão de produto de 2026-07-16 substituiu `padronizar-tabelas-carregamento-incremental`: restaurar footer, contagem e `UPagination` é obrigatório. Este experimento ainda não é evidência de `PASS`, pois preserva wrappers de chrome (`ShellListShell`, `ShellTableFooter`, `MonitoringModuleTable`, `DocsWorkspace`) e detalhe de Documentos em modal, todos proibidos pela regra de migração integral.

## Objetivo

Eliminar CSS/TS de layout paralelo ao template `0f30c09` (customers/index/settings/inbox).

## Feito

| Item | Ação |
|------|------|
| `utils/table-ui.ts` | Só `:ui` literal de customers + HomeSales; **sem** `100dvh` / root max-h / virtualize |
| `ShellListShell` | Melhorou ao retirar sticky, mas deve ser removido e expandido diretamente nas páginas |
| `ShellStickyTableFilters` | Só `flex flex-wrap … gap-1.5` (customers body) |
| `ShellInfiniteTableLoader` | No-op deprecado |
| `ShellTableFooter` | Forma correta, porém wrapper de chrome; o footer deve ser copiado diretamente nas páginas `LIST` |
| `usePagedTable` | Uma página por vez; substitui infinite nas listas |
| `MonitoringModuleTable` | Removeu virtualização/infinite, mas deve ser eliminado e cada rota deve escolher um bundle único |
| Listas (clients, exports, syncs, health, closing, work, docs/imports, monitoring, settings proxies/usage…) | `usePagedTable` + footer; `per_page` 10 |
| Home | bell+plus template; toolbar sem OperationalContext |
| Classes `h-[calc(100dvh…)]` | Removidas das pages |

## Ainda produto (domínio, não chrome inventado)

- Dados API / permissions / types
- `usePagedTable` pode permanecer somente como composable de dados sem UI e após validar paginação server-side
- `ShellListShell` ainda precisa ser removido e expandido nas páginas
- `DocsWorkspace` e detalhe modal precisam ser substituídos por `inbox.vue`
- Home blocos WorkKpis / fiscal shortcuts (conteúdo real no body)
- Auth layout fora do dashboard template

## Comandos

```bash
node frontend/scripts/template-fidelity-gate.mjs   # PASS
pnpm --dir frontend exec vitest run tests/unit/paged-table.test.ts tests/unit/template-fidelity-gate.test.ts
pnpm --dir frontend exec playwright test tests/e2e/smoke.spec.ts --project=desktop-1440
```
