# Evidência de apply — 2026-07-18

## Inventário sanitizado do host

- Hostname reportado: `server`.
- Usuário operacional atual: `obsidian`.
- Caminho do repo: `/home/obsidian/dev/app`.
- Git HEAD atual: `b94c39a9b57f9e83e045c88b6e68069267a47ebd`.
- Docker disponível: `Docker version 29.6.1`.
- Docker Compose disponível: `Docker Compose version v5.3.1`.
- `make`: ausente no host atual.
- DNS de `app.inovaicontabil.com.br`: resolve para `74.214.172.217` com reverso `vps.inovaicontabil.com.br`.
- Portas locais escutando em bind público no host atual: `22`, `3000` e `8080`.
- Portas 80/443 públicas do domínio: `curl` retornou conexão recusada em HTTP e HTTPS no momento da verificação.
- `.env.prod`: ausente.
- `/etc/fiscal-hub/backup.env`: ausente.

## Revisão dos artefatos versionados de produção

- `compose.prod.yml` fixa `APP_URL`, `SESSION_DOMAIN`, `SANCTUM_STATEFUL_DOMAINS` e regra Traefik para `app.inovaicontabil.com.br`.
- `compose.prod.yml` expõe Traefik em 80/443, usa ACME HTTP challenge, healthchecks e serviços produtivos `web`, `php`, `postgres`, `redis`, `horizon`, `scheduler`, `traefik` e `socket-proxy`.
- `PROD_ENV_FILE=.env.prod.example docker compose --env-file .env.prod.example -f compose.prod.yml -p fiscal-hub config --quiet` concluiu com sucesso.
- `docker/nginx/prod.conf` bloqueia `/up` e `/horizon`, define headers de segurança e inclui `Strict-Transport-Security`.
- `Makefile` possui `prod-check`, `prod-config`, `prod-build`, `prod-up`, `prod-backup`, `prod-backup-verify`, `prod-restore`, `prod-restore-smoke`, `prod-readiness` e `prod-release-manifest`.
- `prod-check` bloqueia placeholders, permissões inseguras, SMTP incompleto, `SERPRO_CAPABILITY_*=real`, `SERPRO_KILL_SWITCH=false`, `SERPRO_SMOKE_ENABLED=true`, `FEATURES_GLOBAL_ENABLED=true`, `FEATURES_MUTATING_ENABLED=true` e canais SEFAZ ligados.
- `docker/ops/deploy.sh` classifica instalação fresh/existente, exige confirmação fresh ou `PRE_DEPLOY_BACKUP`, roda migrations de forma controlada, mantém rollback automático de schema proibido e gera manifesto sanitizado.
- `docker/ops/backup.sh` cria backup v3 cifrado quando `BACKUP_PACKAGE_KEY` está definida e não inclui a chave mestra do vault.
- `docker/ops/restore.sh` verifica checksums, pacote v3, paths seguros e exige `BACKUP_PACKAGE_KEY` para abrir backup cifrado.
- `docker/ops/release-manifest.sh` grava manifesto fora do repo, com modo restrito e varredura simples contra padrões sensíveis.
- Diff versionado necessário para cobrir a spec nesta revisão: nenhum.

## Gate de fonte

- `make prod-readiness PHASE=source`: não executado porque `make` está ausente.
- Execução direta equivalente: `PHASE=source ./docker/ops/prod-readiness.sh` com `EVIDENCE_DIR` na change.
- Resultado da execução direta: falhou por `worktree=dirty`.
- Evidência JSON gerada: `openspec/changes/deploy-producao-app-inovaicontabil/evidence/readiness-source-20260718T223708Z.json`.

## Bloqueios atuais para seguir o go-live

- Resolver a worktree suja antes de release por SHA.
- Garantir que o serviço produtivo aceite conexões em 80/443 para ACME e HTTPS.

## Continuação autorizada — presets operacionais

- `make` foi instalado no host.
- `.env.prod` foi criado com modo `600`, chaves fortes geradas localmente, domínio `https://app.inovaicontabil.com.br`, containment fiscal e SMTP presetado para `127.0.0.1`.
- `/etc/fiscal-hub/backup.env` foi criado root-only com modo `600`, `BACKUP_PACKAGE_KEY` distinta e destino `/var/backups/fiscal-hub`.
- Stack dev local foi derrubada; portas públicas `3000` e `8080` ficaram livres.
- `sudo env PROD_ENV=.env.prod BACKUP_ENV=/etc/fiscal-hub/backup.env make prod-check`: OK; runtime SERPRO adiado porque stack prod ainda estava offline.
- `sudo env PROD_ENV=.env.prod BACKUP_ENV=/etc/fiscal-hub/backup.env make prod-config`: OK.
- `sudo env PROD_ENV=.env.prod BACKUP_ENV=/etc/fiscal-hub/backup.env make prod-readiness PHASE=predeploy`: OK; evidência em `/var/lib/fiscal-hub/readiness/readiness-predeploy-20260718T225206Z.json`.
- `sudo env PROD_ENV=.env.prod BACKUP_ENV=/etc/fiscal-hub/backup.env make prod-readiness PHASE=source`: falhou por `worktree=dirty`; evidência em `/var/lib/fiscal-hub/readiness/readiness-source-20260718T225206Z.json`.
- Próximo bloqueio de release: a árvore de trabalho contém alterações não commitadas e a imagem produtiva rotulada por `RELEASE_SHA` não deve ser construída a partir de conteúdo dirty.

## Ajuste de escopo — dados piloto

- O usuário autorizou carregar a massa de `dados/` e usar `felipe@example.com` como login principal.
- A change foi atualizada para remover esse item dos non-goals e exigir seed piloto autorizado.
- `PilotSeeder` e `LocalSerproSmokeSeeder` agora permitem execução fora de `local/testing` somente com `PILOT_SEED_ALLOW_PRODUCTION=true`.
- PFX/PDF permanecem fora de importação automática; o seed cria usuários/offices/clientes conforme `dados/README.md`.
- `.env.prod` recebeu `PILOT_SEED_ALLOW_PRODUCTION=true`.
- Lint via container PHP:
  - `database/seeders/PilotSeeder.php`: sem erro de sintaxe.
  - `database/seeders/LocalSerproSmokeSeeder.php`: sem erro de sintaxe.
- `prod-check` após a flag de seed piloto: OK.
- `prod-readiness PHASE=source` em `2026-07-18T22:54:06Z`: falhou somente por `worktree=dirty`.

## Commit de release e gate source

- Commit criado para estabilizar a worktree: `ca3b3e4d8cc782d433c22706f9e7a65a46186429`.
- `sudo env PROD_ENV=.env.prod BACKUP_ENV=/etc/fiscal-hub/backup.env make prod-readiness PHASE=source`: OK.
- Evidência: `/var/lib/fiscal-hub/readiness/readiness-source-20260718T230928Z.json`.

## Build produtivo

- Commit documental posterior ao gate source: `fa06b6417359834f7d60c821246df865db72b902`.
- `sudo env PROD_ENV=.env.prod BACKUP_ENV=/etc/fiscal-hub/backup.env RELEASE_SHA=fa06b6417359834f7d60c821246df865db72b902 make prod-build`: OK.
- Imagens construídas: `fiscal-hub-php:sha-fa06b6417359` e `fiscal-hub-web:sha-fa06b6417359`.
- Validação de labels OCI reportada pelo Makefile: `OCI revision ok em php e web`.
