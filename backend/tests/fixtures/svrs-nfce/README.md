# Fixtures sanitizadas — SVRS NFC-e DownloadXMLDFe

Sem chave real, CNPJ real, protocolo real, certificado ou XML fiscal de produção.
Parser version: ver `config('sefaz.svrs_nfce_xml.wrapper_parser_version')`.
