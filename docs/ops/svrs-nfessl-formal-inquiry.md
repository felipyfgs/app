# Solicitação formal SVRS/SEFAZ — `NFESSL` Download XML

**Change:** `add-resilient-svrs-nfe55-outbound-xml-retrieval` · task 1.7  
**Status:** rascunho registrado no repositório — **envio institucional pendente** (preencher protocolo/data ao enviar)

## Destinatário sugerido

- Contato técnico/operacional SVRS (portal DFe) e/ou SEFAZ do contribuinte MA  
- Assunto: *Esclarecimentos sobre Download XML com certificado relacionado (NFESSL/NFCESSL)*

## Perguntas

1. O uso **automatizado de baixo volume** (sem navegador, sem CAPTCHA) do formulário `NFESSL/DownloadXMLDFe` e `NFCESSL/DownloadXMLDFe` é autorizado para escritório/contador com A1 relacionado à NF-e/NFC-e?
2. Qual o **limiar** de consultas (por IP, certificado, chave ou CNPJ), janela e **cooldown** em caso de “IP não autorizado devido múltiplas consultas”?
3. O bloqueio aplica-se a **todo o host** (NF-e + NFC-e) ou por formulário/sistema?
4. Há **retenção** máxima do XML disponível no portal e escopo territorial/autorizador?
5. O **POST** direto é suportado ou o **GET** autenticado de sessão é obrigatório?
6. Existe canal M2M/API estável preferível ao formulário HTML?

## Resposta

| Campo | Valor |
|-------|--------|
| Data de envio | _pendente_ |
| Protocolo / e-mail | _pendente_ |
| Resposta formal | _ausente no repositório_ |

Enquanto não houver resposta, o produto opera com **NO_GO de tráfego real** (flags off) e budgets preventivos internos.
